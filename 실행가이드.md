# THE RAUM AS 시스템 실행 가이드 (비개발자용)

## A. 맥북에서 잠깐 테스트해보고 싶을 때

1. **터미널 열기**: `Launchpad → 기타 → 터미널` 실행
2. **압축 해제한 폴더로 이동**
   ```
   cd 다운로드/as_system
   ```
   (실제 저장한 폴더 경로로 바꿔주세요. 폴더를 터미널 창에 끌어다 놓으면 경로가 자동 입력됩니다.)
3. **필요한 프로그램 설치** (최초 1회)
   ```
   pip3 install -r requirements.txt
   ```
4. **담당자 접속코드 설정** (원하는 코드로 바꿔서 입력)
   ```
   export MANAGER_PASSCODE="원하는코드"
   ```
5. **서버 실행**
   ```
   python3 app.py
   ```
6. **브라우저에서 접속**
   - 고객 접수 화면: http://127.0.0.1:5050
   - 담당자 로그인: http://127.0.0.1:5050/login (4단계에서 설정한 접속코드 입력)

터미널을 닫으면 서버도 함께 종료됩니다. 같은 와이파이를 쓰는 다른 사람이 접속하려면 맥북의 방화벽/네트워크 설정이 추가로 필요하며, 맥북 전원이 꺼지면 접속이 끊기므로 **실제 운영은 아래 B번(클라우드 배포)을 권장**합니다.

## B. 실제로 24시간 운영하고 싶을 때 (비용 0원 클라우드 배포)

맥북을 켜두지 않아도 항상 접속 가능한 인터넷 주소(URL)로 운영하려면 클라우드 서버가 필요합니다. 아래 조합은 **완전히 무료이며, 이미 갖고 계신 도메인도 그대로 활용**할 수 있습니다.

> 카페24·가비아 같은 일반 웹호스팅은 이 앱(Flask/Python)을 직접 구동할 수 없어, 앱은 무료 클라우드(Render)에 올리고 기존 도메인은 서브도메인만 연결하는 방식을 사용합니다. 홈페이지 본체나 기존 도메인 설정에는 영향이 없습니다.

### 방법 1 — 저에게 배포를 맡기기 (가장 간단)

Claude 대화 화면에서 **Render** 커넥터를 연결해주시면, 이후 대화에서 제가 직접 서버를 만들고 배포까지 진행한 뒤 접속 가능한 URL을 알려드립니다. 별도의 코딩 지식이 필요 없습니다.

### 방법 2 — 직접 배포하기 (클릭 몇 번)

1. **Turso 무료 계정 생성** (https://turso.tech, 카드 등록 불필요, 약 2분) — 데이터베이스를 1개 만들고 `Database URL`, `Auth Token`을 발급받습니다. 이 값이 있어야 접수 데이터와 사진이 서버 재시작 후에도 사라지지 않습니다.
2. https://render.com 접속 후 무료 회원가입 (GitHub 계정으로 가입하면 더 간편합니다, 카드 불필요)
3. GitHub에 이 폴더를 저장소로 업로드 (GitHub 웹사이트에서 새 저장소를 만들고 파일을 끌어다 놓으면 됩니다)
4. Render 대시보드에서 **New + → Blueprint** 선택 → 방금 만든 저장소 선택
5. Environment 값 입력 후 배포 시작
   - `MANAGER_PASSCODE`: 원하는 담당자 접속코드
   - `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN`: 1번에서 발급받은 값
6. 몇 분 뒤 `https://xxxx.onrender.com` 형태의 주소가 생성되며, 이 주소가 실제 서비스 URL입니다.
7. **기존 도메인 연결(선택)**: 카페24/가비아 도메인 관리 화면에서 서브도메인(예: `as.내도메인.com`)을 6번 주소로 CNAME 연결하면, 발급받은 링크 대신 우리 도메인 주소로 접속할 수 있습니다. Render의 Custom Domain 설정에서 같은 주소를 추가하면 자동으로 보안 인증서(https)가 발급됩니다.

## 자주 묻는 질문

**Q. 매번 `pip3 install`을 해야 하나요?**
A. 아니요. 최초 1회만 설치하면 이후에는 4~5단계(접속코드 설정 + 서버 실행)만 반복하면 됩니다.

**Q. 접수된 데이터는 어디에 저장되나요?**
A. 맥북에서 테스트할 때는 `data/as_system.db` 파일(SQLite)에 저장됩니다. 클라우드(Render) 배포 시에는 `TURSO_DATABASE_URL`을 설정했다면 Turso라는 무료 원격 DB에 영구 저장되고(서버가 재시작되어도 데이터 유지), 설정하지 않았다면 서버 쪽 디스크에 임시로 저장되어 재배포 시 사라질 수 있습니다. **실제 운영 시에는 반드시 Turso 값을 설정해주세요.**

**Q. 담당자 접속코드를 잊어버렸어요.**
A. 로컬 실행 시에는 4단계에서 새 코드로 다시 설정하면 되고, 클라우드(Render) 배포 시에는 Render 대시보드의 Environment 탭에서 `MANAGER_PASSCODE` 값을 언제든 변경할 수 있습니다.
